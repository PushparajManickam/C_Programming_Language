#include"stdio.h"
int main()
{
float pi=3.14,circle,radius;
printf("Enter the radius\t: ");
scanf("%f",&radius);
circle = (pi * radius * radius);
printf("Area of a circle \t: %f",circle);       //Area of a circle = (pi * radius * radius)
printf("\nBY PUSHPARAJ MANICKAM!!!\n");
return 0;
}
//BY PUSHPARAJ MANICKAM