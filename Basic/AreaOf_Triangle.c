#include"stdio.h"
int main()
{
float Triangle,Base,Height;
printf("Enter the Height\t: ");
scanf("%f",&Height);
printf("Enter the Base\t\t: ");
scanf("%f",&Base);
Triangle = (Height * Base) / 2;
printf("Area of a triangle\t: %f",Triangle);  ////Area of a Triangle = (Height * Base) / 2
printf("\nBY PUSHPARAJ MANICKAM!!!\n");
return 0;
}
//BY PUSHPARAJ MANICKAM